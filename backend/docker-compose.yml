version: "3"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    depends_on:
      - db

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: "1123"
      MYSQL_DATABASE: "internet_tv"
      MYSQL_USER: "root"
      MYSQL_PASSWORD: "1123"
